import SwiftUI

struct HomeView: View {
    var body: some View {
        NavigationView {
            Text("A penny saved is a penny earned!")
                .navigationTitle("Home")
        }
    }
}

